<section
  class="custom-editorial-promo-banner"
  {% if section.settings.range_tab_key != blank %}data-range-panel-key="{{ section.settings.range_tab_key | escape }}"{% endif %}
  style="
    --custom-editorial-promo-banner-max-width: {{ section.settings.max_width }}px;
    --custom-editorial-promo-banner-background: {{ section.settings.background_color }};
    --custom-editorial-promo-banner-card-background: {{ section.settings.card_background_color }};
    --custom-editorial-promo-banner-heading: {{ section.settings.heading_color }};
    --custom-editorial-promo-banner-body: {{ section.settings.body_color }};
    --custom-editorial-promo-banner-accent: {{ section.settings.accent_color }};
    --custom-editorial-promo-banner-button-text: {{ section.settings.button_text_color }};
    --custom-editorial-promo-banner-button-bg: {{ section.settings.button_background_color }};
    --custom-editorial-promo-banner-button-border: {{ section.settings.button_border_color }};
    --custom-editorial-promo-banner-radius: {{ section.settings.banner_radius }}px;
    --custom-editorial-promo-banner-height: {{ section.settings.banner_height }}px;
    --custom-editorial-promo-banner-height-mobile: {{ section.settings.banner_height_mobile }}px;
    --custom-editorial-promo-banner-padding-top: {{ section.settings.padding_top }}px;
    --custom-editorial-promo-banner-padding-bottom: {{ section.settings.padding_bottom }}px;
    --custom-editorial-promo-banner-padding-top-mobile: {{ section.settings.padding_top_mobile }}px;
    --custom-editorial-promo-banner-padding-bottom-mobile: {{ section.settings.padding_bottom_mobile }}px;
    --custom-editorial-promo-banner-content-width: {{ section.settings.content_width }}px;
    --custom-editorial-promo-banner-content-padding: {{ section.settings.content_padding }}px;
    --custom-editorial-promo-banner-content-padding-mobile: {{ section.settings.content_padding_mobile }}px;
  "
  {{ section.shopify_attributes }}
>
  <div class="custom-editorial-promo-banner__inner">
    {%- if section.settings.section_heading != blank -%}
      <header class="custom-editorial-promo-banner__header">
        <h2 class="custom-editorial-promo-banner__section-heading">{{ section.settings.section_heading | escape }}</h2>
      </header>
    {%- endif -%}

    <div class="custom-editorial-promo-banner__card">
      <div class="custom-editorial-promo-banner__media">
        {%- if section.settings.mobile_image != blank -%}
          <picture>
            <source
              media="(max-width: 749px)"
              srcset="
                {{ section.settings.mobile_image | image_url: width: 480 }} 480w,
                {{ section.settings.mobile_image | image_url: width: 720 }} 720w,
                {{ section.settings.mobile_image | image_url: width: 960 }} 960w
              "
              sizes="100vw"
            >
            {%- if section.settings.image != blank -%}
              {{
                section.settings.image
                | image_url: width: 3200
                | image_tag:
                  loading: 'lazy',
                  widths: '768,1200,1600,2000,2400,3200',
                  sizes: '100vw',
                  class: 'custom-editorial-promo-banner__image',
                  alt: section.settings.image.alt
              }}
            {%- else -%}
              {{
                section.settings.mobile_image
                | image_url: width: 3200
                | image_tag:
                  loading: 'lazy',
                  widths: '768,1200,1600,2000,2400,3200',
                  sizes: '100vw',
                  class: 'custom-editorial-promo-banner__image',
                  alt: section.settings.mobile_image.alt
              }}
            {%- endif -%}
          </picture>
        {%- elsif section.settings.image != blank -%}
          {{
            section.settings.image
            | image_url: width: 3200
            | image_tag:
              loading: 'lazy',
              widths: '768,1200,1600,2000,2400,3200',
              sizes: '100vw',
              class: 'custom-editorial-promo-banner__image',
              alt: section.settings.image.alt
          }}
        {%- else -%}
          {{ 'lifestyle-2' | placeholder_svg_tag: 'custom-editorial-promo-banner__image custom-editorial-promo-banner__image--placeholder' }}
        {%- endif -%}
      </div>

      <div class="custom-editorial-promo-banner__overlay"></div>

      <div class="custom-editorial-promo-banner__content">
        {%- if section.settings.eyebrow != blank -%}
          <p class="custom-editorial-promo-banner__eyebrow">{{ section.settings.eyebrow | escape }}</p>
        {%- endif -%}
        {%- if section.settings.heading != blank -%}
          <h3 class="custom-editorial-promo-banner__heading">{{ section.settings.heading | escape }}</h3>
        {%- endif -%}
        {%- if section.settings.text != blank -%}
          <div class="custom-editorial-promo-banner__text">
            {{ section.settings.text | escape | newline_to_br }}
          </div>
        {%- endif -%}
        {%- if section.settings.button_label != blank -%}
          {%- if section.settings.button_link != blank -%}
            <a class="custom-editorial-promo-banner__button" href="{{ section.settings.button_link }}">
              {{ section.settings.button_label | escape }}
            </a>
          {%- else -%}
            <span class="custom-editorial-promo-banner__button custom-editorial-promo-banner__button--static">
              {{ section.settings.button_label | escape }}
            </span>
          {%- endif -%}
        {%- endif -%}
      </div>
    </div>
  </div>
</section>

{% stylesheet %}
  .custom-editorial-promo-banner {
    background: var(--custom-editorial-promo-banner-background);
  }

  .custom-editorial-promo-banner__inner {
    margin: 0 auto;
    max-width: var(--custom-editorial-promo-banner-max-width);
    padding:
      var(--custom-editorial-promo-banner-padding-top-mobile)
      16px
      var(--custom-editorial-promo-banner-padding-bottom-mobile);
  }

  .custom-editorial-promo-banner__header {
    margin-bottom: 1.5rem;
    text-align: center;
  }

  .custom-editorial-promo-banner__section-heading {
    color: var(--custom-editorial-promo-banner-heading);
    margin: 0;
  }

  .custom-editorial-promo-banner__card {
    background: var(--custom-editorial-promo-banner-card-background);
    border-radius: var(--custom-editorial-promo-banner-radius);
    min-height: var(--custom-editorial-promo-banner-height-mobile);
    overflow: hidden;
    position: relative;
  }

  .custom-editorial-promo-banner__media,
  .custom-editorial-promo-banner__overlay {
    inset: 0;
    position: absolute;
  }

  .custom-editorial-promo-banner__image,
  .custom-editorial-promo-banner__image--placeholder {
    display: block;
    height: 100%;
    object-fit: cover;
    width: 100%;
  }

  .custom-editorial-promo-banner__overlay {
    background:
      linear-gradient(90deg, rgb(255 253 248 / 0.92) 0%, rgb(255 253 248 / 0.68) 28%, rgb(255 253 248 / 0) 58%),
      linear-gradient(180deg, rgb(255 253 248 / 0.08), rgb(255 253 248 / 0.04));
  }

  .custom-editorial-promo-banner__content {
    align-items: flex-start;
    box-sizing: border-box;
    display: flex;
    flex-direction: column;
    gap: 0.8rem;
    justify-content: center;
    min-height: var(--custom-editorial-promo-banner-height-mobile);
    padding: var(--custom-editorial-promo-banner-content-padding-mobile);
    position: relative;
    width: min(100%, var(--custom-editorial-promo-banner-content-width));
    z-index: 1;
  }

  .custom-editorial-promo-banner__eyebrow {
    color: var(--custom-editorial-promo-banner-heading);
    font-size: 0.7rem;
    letter-spacing: 0.14em;
    margin: 0;
    text-transform: uppercase;
  }

  .custom-editorial-promo-banner__heading {
    color: var(--custom-editorial-promo-banner-heading);
    margin: 0;
  }

  .custom-editorial-promo-banner__text {
    color: var(--custom-editorial-promo-banner-body);
    line-height: 1.55;
  }

  .custom-editorial-promo-banner__button {
    align-items: center;
    background: var(--custom-editorial-promo-banner-button-bg);
    border: 1px solid var(--custom-editorial-promo-banner-button-border);
    border-radius: 999px;
    color: var(--custom-editorial-promo-banner-button-text);
    display: inline-flex;
    justify-content: center;
    min-height: 2.75rem;
    padding: 0.8rem 1.15rem;
    text-decoration: none;
  }

  .custom-editorial-promo-banner__button--static {
    cursor: default;
  }

  @media screen and (min-width: 750px) {
    .custom-editorial-promo-banner__inner {
      padding:
        var(--custom-editorial-promo-banner-padding-top)
        48px
        var(--custom-editorial-promo-banner-padding-bottom);
    }

    .custom-editorial-promo-banner__card,
    .custom-editorial-promo-banner__content {
      min-height: var(--custom-editorial-promo-banner-height);
    }

    .custom-editorial-promo-banner__content {
      padding: var(--custom-editorial-promo-banner-content-padding);
    }
  }
{% endstylesheet %}

{% schema %}
{
  "name": "Editorial promo banner",
  "tag": "section",
  "class": "section-wrapper",
  "disabled_on": {
    "groups": ["header", "footer"]
  },
  "settings": [
    {
      "type": "text",
      "id": "section_heading",
      "label": "Section heading",
      "default": "Tea Inspired"
    },
    {
      "type": "text",
      "id": "range_tab_key",
      "label": "Range tab key",
      "info": "Match a tab target key from the Range Hero to show this section when that pill is selected."
    },
    {
      "type": "image_picker",
      "id": "image",
      "label": "Banner image"
    },
    {
      "type": "image_picker",
      "id": "mobile_image",
      "label": "Mobile banner image"
    },
    {
      "type": "text",
      "id": "eyebrow",
      "label": "Eyebrow",
      "default": "Elixir Collection"
    },
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "Elevated Tea Experiences"
    },
    {
      "type": "textarea",
      "id": "text",
      "label": "Text",
      "default": "Crafted from handpicked Ceylon tea - perfect for iced teas, infused waters, tea mixology, shakes and smoothies."
    },
    {
      "type": "text",
      "id": "button_label",
      "label": "Button label",
      "default": "Discover Elixir"
    },
    {
      "type": "url",
      "id": "button_link",
      "label": "Button link"
    },
    {
      "type": "color",
      "id": "background_color",
      "label": "Background color",
      "default": "#fffdf8"
    },
    {
      "type": "color",
      "id": "card_background_color",
      "label": "Banner base color",
      "default": "#f7ede6"
    },
    {
      "type": "color",
      "id": "heading_color",
      "label": "Heading color",
      "default": "#1f1812"
    },
    {
      "type": "color",
      "id": "body_color",
      "label": "Body color",
      "default": "#4b4137"
    },
    {
      "type": "color",
      "id": "accent_color",
      "label": "Accent color",
      "default": "#b08a2f"
    },
    {
      "type": "color",
      "id": "button_text_color",
      "label": "Button text color",
      "default": "#ffffff"
    },
    {
      "type": "color",
      "id": "button_background_color",
      "label": "Button background color",
      "default": "#b08a2f"
    },
    {
      "type": "color",
      "id": "button_border_color",
      "label": "Button border color",
      "default": "#b08a2f"
    },
    {
      "type": "range",
      "id": "max_width",
      "label": "Max width",
      "min": 640,
      "max": 1800,
      "step": 20,
      "unit": "px",
      "default": 1440
    },
    {
      "type": "range",
      "id": "banner_height",
      "label": "Desktop banner height",
      "min": 260,
      "max": 700,
      "step": 10,
      "unit": "px",
      "default": 420
    },
    {
      "type": "range",
      "id": "banner_height_mobile",
      "label": "Mobile banner height",
      "min": 220,
      "max": 600,
      "step": 10,
      "unit": "px",
      "default": 320
    },
    {
      "type": "range",
      "id": "banner_radius",
      "label": "Banner radius",
      "min": 0,
      "max": 40,
      "step": 1,
      "unit": "px",
      "default": 12
    },
    {
      "type": "range",
      "id": "content_width",
      "label": "Content width",
      "min": 220,
      "max": 700,
      "step": 10,
      "unit": "px",
      "default": 360
    },
    {
      "type": "range",
      "id": "content_padding",
      "label": "Content padding",
      "min": 20,
      "max": 120,
      "step": 4,
      "unit": "px",
      "default": 56
    },
    {
      "type": "range",
      "id": "content_padding_mobile",
      "label": "Content padding mobile",
      "min": 16,
      "max": 64,
      "step": 4,
      "unit": "px",
      "default": 24
    },
    {
      "type": "range",
      "id": "padding_top",
      "label": "Padding top",
      "min": 0,
      "max": 120,
      "step": 4,
      "unit": "px",
      "default": 8
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "label": "Padding bottom",
      "min": 0,
      "max": 120,
      "step": 4,
      "unit": "px",
      "default": 24
    },
    {
      "type": "range",
      "id": "padding_top_mobile",
      "label": "Padding top mobile",
      "min": 0,
      "max": 80,
      "step": 4,
      "unit": "px",
      "default": 16
    },
    {
      "type": "range",
      "id": "padding_bottom_mobile",
      "label": "Padding bottom mobile",
      "min": 0,
      "max": 80,
      "step": 4,
      "unit": "px",
      "default": 20
    }
  ],
  "presets": [
    {
      "name": "Editorial promo banner"
    }
  ]
}
{% endschema %}
